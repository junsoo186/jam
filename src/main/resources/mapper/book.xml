<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam.repository.interfaces.BookRepository">

	<insert id="insertBook">
		INSERT INTO book_tb (user_id, title, author_comment, author,
		book_cover_image, age)
		VALUES (#{userId}, #{title}, #{authorComment}, #{author}, #{bookCoverImage},
		#{age});
	</insert>

	<insert id="insertBookCategories">
		<foreach item="categoryId" collection="categoryIds"
			separator=";">
			INSERT INTO book_category_tb (book_id, category_id)
			VALUES
			(#{bookId}, #{categoryId});
		</foreach>
	</insert>

	<insert id="insertBookGenres">
		<foreach item="genreId" collection="genreIds" separator=";">
			INSERT INTO book_genre_tb (book_id, genre_id)
			VALUES (#{bookId},
			#{genreId});
		</foreach>
	</insert>

	<insert id="insertBookTags">
		<foreach item="tagId" collection="tagIds" separator=";">
			INSERT
			INTO book_tag_tb (book_id, tag_id)
			VALUES (#{bookId}, #{tagId});
		</foreach>
	</insert>

	<select id="AllBookList"
		resultType="com.jam.repository.model.Book">
		select title, author, book_cover_image,
		category_id,
		genre_id, tag_id, likes
		from book_tb
	</select>

	<select id="AllBookListByUserId"
		resultType="com.jam.repository.model.Book">
		select title, author, book_cover_image,
		category_id,
		genre_id, tag_id, likes
		from book_tb
		where user_id = #{userId}
	</select>

	<select id="detailBookByBookId"
		resultType="com.jam.repository.model.Book">
		select title, author, author_comment, introduction,
		book_cover_image, category_id, genre_id, tag_id, serial_day, likes,
		age
		from book_tb
		where book_id = #{bookId};
	</select>

	<update id="updateBook">
		update book_tb set
		title = #{title}, author_comment =
		#{authorComment}, book_cover_image =
		#{bookCoverImage},
		category_id =
		#{categoryId}, genre_id = #{genreId}, tag_id = #{tagId},
		serial_day =
		#{serialDay}, age = #{age}
		where book_id = #{bookId}
	</update>

	<update id="bookViewCountUp">
		update book_tb set
		SET view = view + 1
		where book_id =
		#{bookId}
	</update>

	<delete id="deleteBook">
		delete book_tb where book_id = #{bookId}
	</delete>

	<insert id="insertTagName">
		insert into tag_tb (tag_name) value (#{tagName})
	</insert>

</mapper>